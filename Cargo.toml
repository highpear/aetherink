[package]
name = "aetherink"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = { version = "0.33", features = ["persistence"] }
egui = "0.33"
serde = { version = "1", features = ["derive"] }
